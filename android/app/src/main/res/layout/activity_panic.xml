<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="@color/starling_bg">

    <TextView
        android:id="@+id/panic_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/panic_connect_title"
        android:textColor="@color/starling_fg"
        android:textSize="22sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/panic_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:textColor="@color/starling_fg_dim"
        android:textSize="15sp" />

    <Button
        android:id="@+id/panic_allow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:text="@string/panic_allow" />

    <Button
        android:id="@+id/panic_deny"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/panic_deny" />
</LinearLayout>
